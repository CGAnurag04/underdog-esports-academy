{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "⚡ Run Underdog Esports Academy (Starts Server & Opens Chrome)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/backend/server.js",
      "cwd": "${workspaceFolder}/backend",
      "console": "integratedTerminal"
    }
  ]
}
